<% if @problem %>
  <div class="alert alert-danger" role="alert">
    <p>Connexion impossible pour la (ou les) raison(s) suivante(s) :<p>
    <ul>
    <% @logged_in.errors.full_messages.each do |message| %>
      <li><%= message %></li>

    <% end %>
  </ul>
  </div>
  
<% end %>

<br>
<br>

<h1 class="text-center">Se connecter</h1>
<br>
<div class="container text-left">
  <form action="<%= sessions_path %>" method="POST">
    <input type="hidden" name="authenticity_token" value=<%= form_authenticity_token %> >
<br>
  <div class="form-group">
    <label for="email">Adresse Email</label>
      <input type="text" class="form-control" name='email' placeholder="Insère ton email ici">
    <br>
    <label for="password">Mot de passe</label>
      <input type="password" class="form-control" name='password'placeholder="Renseigne ton mot de passe ici"></inputtype>
  </div>
  <button type="submit" class="btn btn-dark">Login</button>
</form>
</div>